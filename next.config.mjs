/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['letsenhance.io', 'cdn-icons-png.freepik.com', 'media2.giphy.com', 'media3.giphy.com' ,'media4.giphy.com', 'media1.giphy.com'],
    },
};

export default nextConfig;
